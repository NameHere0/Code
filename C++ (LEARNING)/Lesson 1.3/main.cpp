#include <iostream> // for std::cout

int main()
{
    std::cout << 5;       // print the literal number `5`
    std::cout << -6.8;    // print the literal number `-6.~~7~~ (because i know there are going to be jokes about it)`
    std::cout << 'H';     // print the literal character `H`
    std::cout << "Hello"; // print the literal text `Hello`
    std::cout << "What if i\n";
    std::cout << "Does this count as text?";

    return 0;
}
